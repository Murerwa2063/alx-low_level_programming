#include <stdio.h>
#include <stdlib.h>
/**
 * main - Starting point
 *
 * Return:
 * 0 (success)
 * non-zero if an error occurred
 */
int main(void)

{
char i;
	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
		return (0);
}
