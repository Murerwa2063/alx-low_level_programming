#include <stdio.h>
#include <stdlib.h>
/**
 * main - Starting point
 *
 * Return:
 * 0 (success)
 * non-zero if an error occurred
 */
main(void)

{
int ch;
	for (ch = "a"; ch <= "z"; ch++)
		putchar(ch);
	putchar("\n")
		return (0);
}
