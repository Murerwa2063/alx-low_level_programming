#include <stdio.h>
#include <stdlib.h>
/**
 * main - Starting point
 *
 * Return:
 * 0 (success)
 * non-zero if an error occurred
 */
int main(void)

{
char c;
	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	putchar('\n');
		return (0);
}
