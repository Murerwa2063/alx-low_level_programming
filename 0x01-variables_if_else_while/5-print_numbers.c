#include <stdio.h>
/**
 * main - Starting point
 * Return:
 * 0 (success)
 * non-zero if an error occurred
 */

int main(void)
{
int i;
for (i = 0; i < 10; i++)
	printf("%d", i);
putchar(i);

return (0);
}
