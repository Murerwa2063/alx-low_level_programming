#include "main.h"

/**
  * _abs - Computes the absolute value of an integer
  * @n: integer being computed
  * Return:
  * int
  * int * negative
  */

int _abs(int n)

{

	if (n > 0)
		return (n);
	else
		return (n * -1);
}
